Decompose the content below into a list of propositions: distinct, minimal, self-contained statements of fact or instruction. Each proposition must be understandable on its own — replace pronouns and references ("it", "this section", "the above") with the concrete names they refer to — and must not merge two separate ideas. Preserve exact identifiers, values, paths and names verbatim.

Document location: {{breadcrumb}}

Produce at most {{max_propositions}} propositions, in document order. Output valid JSON matching this schema:
{
  "propositions": string[]
}

Do not include anything outside the JSON object.

Content:
---
{{content}}
---
